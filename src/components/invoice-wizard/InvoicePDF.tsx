// PDF generation stub — react-pdf excluded from Vercel server build.
// Replace with html-to-pdf API (e.g. docuseal, pdfshift, or server-side puppeteer).
export default function InvoicePDF({ invoice }: { invoice: any }) {
  return null
}